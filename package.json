{
  "name": "lexafric-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Projet vitrine Lexafric - Front multilingue FR/EN/AR",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "lint": "pnpm --filter web lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
